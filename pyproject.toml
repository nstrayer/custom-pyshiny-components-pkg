[build-system]
requires = ["setuptools>=40.6.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "customShinyComponents"
version = "0.1"
dependencies = ["shiny >= 0.6.0"]

[tool.setuptools]
packages = ["customShinyComponents"]
include-package-data = true
zip-safe = false